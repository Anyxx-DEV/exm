# Änderungen committen und pushen

Führe diese Befehle in PowerShell aus (im Projektordner):

```powershell
# Stelle sicher, dass du im richtigen Ordner bist
cd "C:\Users\timlu\Downloads\winutil-main\winutil-main"

# Alle neuen/geänderten Dateien hinzufügen (inkl. Bild)
git add .

# Änderungen committen
git commit -m "Add EXM screenshot and update README"

# Auf GitHub hochladen
git push
```

Das war's! Dein Bild wird jetzt auf GitHub angezeigt.

